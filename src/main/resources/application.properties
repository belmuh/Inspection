spring.application.name=inspection

# H2 Database Configuration
#spring.datasource.url=jdbc:h2:mem:inspection_db
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# H2 Web Console
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# JPA Configuration for H2
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/inspection
spring.datasource.username=belma
spring.datasource.password=1
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SQL Initialization
spring.sql.init.mode=always
spring.sql.init.continue-on-error=false

# Logging
logging.level.com.company.inspection=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.file.name=logs/application.log

# Server
server.port=8080

# JSON formatting
spring.jackson.serialization.indent-output=true

# Actuator
management.endpoints.web.exposure.include=health,info,metrics

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.packages-to-scan=com.company.inspection.controller
